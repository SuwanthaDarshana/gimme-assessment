{
  "name": "gimme-assessment",
  "private": true,
  "type": "module",
  "scripts": {
    "seed": "npm --prefix server run seed",
    "test": "npm --prefix server run test",
    "start": "npm --prefix server run start",
    "server": "npm --prefix server run dev",
    "client": "npm --prefix client run dev"
  }
}
